# .NET build output
bin/
obj/

# SQLite database (created at runtime by EF migrations)
*.db
*.db-shm
*.db-wal

# Node / Vite
node_modules/
ClientApp/dist/

# Built SPA served by the backend (produced by `npm run build`)
TaskManager/wwwroot/

# IDE / OS
.vs/
.idea/
.DS_Store
